pyyaml
# Flask, SQLAlchemy, flask-sqlalchemy, httpx, and aiohttp appear only as
# scaffold/prompt text for the C4/C5 mocked target app fed to sub-models —
# the functional harness mocks them; they are not real runtime dependencies.